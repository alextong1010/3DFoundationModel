# mode
adapt: false
use_pretrained: true
use_mlp: true

# dirs
display_name: pretrained_mlp_red_circle
adapt_dataset_path_dir: push_circle_dataset_blue
dataset_path_dir: push_circle_dataset_red
models_save_dir: trained_models_pretrained_mlp_red_circle
output_dir: output_pretrained_mlp_red_circle

# hyperparams
num_epochs: 500
num_diffusion_iters: 100
num_tests: 200
num_vis_demos: 3
num_train_demos: 200
num_warmup_steps: 500
pred_horizon: 16
obs_horizon: 2
action_horizon: 8
max_steps: 300
eval_epoch: 20
lr: 0.0001
weight_decay: 0.000001
batch_size: 64
resize_scale: 224
wandb: true
verbose: false
